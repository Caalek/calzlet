tasks:
  - name: server
    before: |
      cd server
    init: |
      echo npm install
    command: |
      echo npm run dev
  - name: client
    before: |
      cd client
    init: |
      echo npm install
    command: |
      echo npm start

ports:
  - port: 3000
    onOpen: open-preview